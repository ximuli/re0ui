<template>
  <button class="re0-button" :class="{ [`re0-theme-${theme}`]: theme }">
    <slot></slot>
  </button>
</template>

<script>
export default {
  props: {
    theme: {
      type: String,
      default: 'button'
    }
  }
}
</script>

<style lang="scss">
@import './../assets/style/variable';

$h: 32px;
$border-color: #d9d9d9;
$color: #333;
$radius: 4px;

.re0-button {
  box-sizing: border-box; height: $h; padding: 0 12px; cursor: pointer;
  display: inline-flex; justify-content: center; align-items: center;
  white-space: nowrap; background: white; color: $color;
  border: 1px solid $border-color; border-radius: $radius;
  box-shadow: 0 1px 0 fade-out($color: black, $amount: 0.95);
  & + & {
    margin-left: 8px;
  }
  &:hover,&:focus {
    color: $colorEmilia; border-color: $colorEmilia;
  }
  &:focus {
    outline: none;
  }
  &::-moz-focus-inner {
    border: 0;
  }
}
</style>